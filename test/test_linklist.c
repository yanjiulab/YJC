#include "test.h"
#include "linklist.h"

void test_linklist() {
    list *l = list_new();
    // listnode_add(l, 1);
    // listnode_add(l, 11);
    // listnode_add(l, 12);
    // listnode_add(l, 133);
    // listnode_add(l, "abc");
    // list_dump(l, "%d");
    // listnode_delete(l, 1);
    // list_dump(l, "%d");
}