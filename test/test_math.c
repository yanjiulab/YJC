#include <stdio.h>

#include "defs.h"
#include "test.h"
void test_math() {
    int a = 1;
    int b = -2;
    int c = MAX(a, b);
    // printf("max(a,b): %d\n", max(a, b));
    // printf("min(a,b): %d\n", min(a, b));
    // printf("abs(a): %d\n", abs(a));
    // printf("abs(b): %d\n", abs(b));
    // printf("nabs(a): %d\n", nabs(a));
    // printf("nabs(b): %d\n", nabs(b));
}