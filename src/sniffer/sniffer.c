#include "sniffer.h"

// sniffer_t* sniffer_new(char* device_name, enum direction_t dirc) {
//     sniffer_t snif = calloc(1, sizeof(sniffer_t));
//     snif.psock = packet_socket_new(device_name);
// }